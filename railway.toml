# Railway deployment config for the frontend service
# Backend service uses backend/railway.toml via root directory setting

[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm --filter frontend start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
