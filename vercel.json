{
  "version": 2,
  "buildCommand": "pnpm --filter frontend build",
  "outputDirectory": "frontend/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_API_URL": "@spotlightready_api_url"
  }
}
